<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Campus Event Hub</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .dashboard-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
        }
        
        .welcome-section {
            text-align: center;
            padding: 2rem;
            background: linear-gradient(135deg, #007bff 0%, #0056b3 100%);
            color: white;
            border-radius: 12px;
            margin-bottom: 2rem;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        }
        
        .welcome-section h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }
        
        .welcome-section p {
            font-size: 1.2rem;
            max-width: 700px;
            margin: 0 auto 1.5rem;
        }
        
        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }
        
        .dashboard-card {
            background: white;
            border-radius: 12px;
            padding: 1.75rem;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            display: flex;
            flex-direction: column;
            border: 1px solid rgba(0,0,0,0.05);
        }
        
        .dashboard-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
        }
        
        .dashboard-card h3 {
            margin-top: 0;
            color: var(--primary-color);
            font-size: 1.4rem;
            font-weight: 600;
            margin-bottom: 1rem;
        }
        
        .dashboard-card p {
            color: var(--text-secondary);
            flex-grow: 1;
        }
        
        .dashboard-card .btn {
            margin-top: 1rem;
            align-self: flex-start;
            min-height: 44px; /* Better touch targets */
            min-width: 44px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }
        
        .recent-events {
            background: var(--bg-white);
            border-radius: 12px;
            padding: 1.75rem;
            box-shadow: 0 4px 20px var(--shadow-light);
            margin-bottom: 2rem;
        }
        
        .section-title {
            color: var(--primary-color);
            font-size: 1.8rem;
            margin-top: 0;
            margin-bottom: 1.5rem;
            text-align: center;
        }
        
        .events-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 1.5rem;
        }
        
        .event-item {
            background: var(--bg-light);
            border-radius: 8px;
            padding: 1.25rem;
            border: 1px solid var(--border-color);
        }
        
        .event-item h4 {
            margin-top: 0;
            color: var(--primary-color);
            font-size: 1.1rem;
        }
        
        .event-item p {
            margin: 0.5rem 0;
            color: var(--text-secondary);
            font-size: 0.9rem;
        }
        
        .no-events {
            text-align: center;
            padding: 2rem;
            color: var(--text-light);
            grid-column: 1 / -1;
        }
        
        /* Enhanced responsive design */
        @media (max-width: 1200px) {
            .dashboard-container {
                max-width: 95%;
                padding: 1.5rem;
            }
            
            .dashboard-grid {
                grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            }
            
            .events-list {
                grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            }
        }
        
        @media (max-width: 992px) {
            .dashboard-container {
                padding: 1.5rem;
            }
            
            .welcome-section h1 {
                font-size: 2.2rem;
            }
            
            .welcome-section p {
                font-size: 1.1rem;
            }
            
            .dashboard-grid {
                grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            }
            
            .section-title {
                font-size: 1.6rem;
            }
        }
        
        @media (max-width: 768px) {
            .dashboard-container {
                padding: 1rem;
            }
            
            .welcome-section {
                padding: 1.5rem;
            }
            
            .welcome-section h1 {
                font-size: 2rem;
            }
            
            .welcome-section p {
                font-size: 1rem;
            }
            
            .dashboard-grid {
                grid-template-columns: 1fr;
            }
            
            .dashboard-card {
                padding: 1.5rem;
            }
            
            .section-title {
                font-size: 1.5rem;
            }
            
            .recent-events {
                padding: 1.5rem;
            }
        }
        
        @media (max-width: 576px) {
            .dashboard-container {
                padding: 0.75rem;
            }
            
            .welcome-section {
                padding: 1.25rem;
            }
            
            .welcome-section h1 {
                font-size: 1.8rem;
            }
            
            .welcome-section p {
                font-size: 0.95rem;
            }
            
            .dashboard-card {
                padding: 1.25rem;
            }
            
            .dashboard-card h3 {
                font-size: 1.3rem;
            }
            
            .section-title {
                font-size: 1.3rem;
            }
            
            .recent-events {
                padding: 1.25rem;
            }
            
            .events-list {
                grid-template-columns: 1fr;
            }
            
            .event-item {
                padding: 1rem;
            }
        }
        
        @media (max-width: 480px) {
            .dashboard-container {
                padding: 0.5rem;
            }
            
            .welcome-section {
                padding: 1rem;
            }
            
            .welcome-section h1 {
                font-size: 1.6rem;
            }
            
            .welcome-section p {
                font-size: 0.9rem;
            }
            
            .dashboard-card {
                padding: 1rem;
            }
            
            .dashboard-card h3 {
                font-size: 1.2rem;
            }
            
            .section-title {
                font-size: 1.2rem;
            }
            
            .recent-events {
                padding: 1rem;
            }
            
            .event-item {
                padding: 0.8rem;
            }
            
            .event-item h4 {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <%- include('header') %>

    <main>
        <div class="dashboard-container">
            <section class="welcome-section fade-in">
                <h1>Welcome to Your Dashboard</h1>
                <p>Manage your events, registrations, and profile all in one place</p>
                <% if (typeof user !== 'undefined' && user) { %>
                    <p>Hello, <strong><%= user.name %></strong>! You are logged in as <strong><%= user.role %></strong>.</p>
                <% } else { %>
                    <p>You are currently browsing as a guest. <a href="/auth/login" style="color: white; text-decoration: underline;">Login</a> or <a href="/auth/register" style="color: white; text-decoration: underline;">Register</a> to access all features.</p>
                <% } %>
            </section>
            
            <div class="dashboard-grid">
                <div class="dashboard-card fade-in">
                    <h3> Browse Events </h3>
                    <p>Discover and explore all upcoming campus events. Find something that interests you!</p>
                    <a href="/events" class="btn btn-primary">View All Events</a>
                </div>
                
                <div class="dashboard-card fade-in">
                    <h3> My Registrations </h3>
                    <p>View and manage your event registrations. See upcoming events you've signed up for.</p>
                    <a href="/student/registrations" class="btn btn-primary">View Registrations</a>
                </div>
                
                <div class="dashboard-card fade-in">
                    <h3> My Events </h3>
                    <p>Create, manage, and track your events. See registration statistics and feedback.</p>
                    <a href="/organizer/my-events" class="btn btn-primary">Manage Events</a>
                </div>
                
                <div class="dashboard-card fade-in">
                    <h3> Create Event </h3>
                    <p>Organize your own campus event. Set date, time, location, and description.</p>
                    <a href="/organizer" class="btn btn-primary">Create New Event</a>
                </div>
                
                <div class="dashboard-card fade-in">
                    <h3> Admin Panel </h3>
                    <p>Manage users, events, and system settings. View analytics and reports.</p>
                    <a href="/admin" class="btn btn-primary">Access Admin Panel</a>
                </div>
                
                <% if (typeof user !== 'undefined' && user) { %>
                <div class="dashboard-card fade-in">
                    <h3> Profile Settings </h3>
                    <p>Update your personal information, change password, and manage preferences.</p>
                    <a href="/profile" class="btn btn-primary">Edit Profile</a>
                </div>
                <% } else { %>
                <div class="dashboard-card fade-in">
                    <h3> Account Access </h3>
                    <p>Create an account or login to access personalized features and manage your events.</p>
                    <a href="/auth/login" class="btn btn-primary">Login</a>
                    <a href="/auth/register" class="btn btn-secondary" style="margin-left: 0.5rem;">Register</a>
                </div>
                <% } %>
            </div>
            
            <section class="recent-events fade-in">
                <h2 class="section-title">Recent Events</h2>
                <% if (typeof events !== 'undefined' && events && events.length > 0) { %>
                    <div class="events-list">
                        <% events.slice(0, 6).forEach(function(event) { %>
                            <div class="event-item">
                                <h4><%= event.title %></h4>
                                <p><strong>Date:</strong> <%= event.date %> at <%= event.time %></p>
                                <p><strong>Location:</strong> <%= event.location %></p>
                                <% if (event.fee && event.fee > 0) { %>
                                    <p><strong>Fee:</strong> <span style="color: #28a745; font-weight: bold;">UGX <%= event.fee.toLocaleString() %></span></p>
                                <% } else { %>
                                    <p><strong>Fee:</strong> <span style="color: #007bff; font-weight: bold;">Free</span></p>
                                <% } %>
                                <p><%= event.description.substring(0, 100) %>...</p>
                                <a href="/register/<%= event._id %>" class="btn btn-primary" style="padding: 0.5rem 1rem; font-size: 0.9rem;">Register</a>
                            </div>
                        <% }); %>
                    </div>
                <% } else { %>
                    <div class="no-events">
                        <h3>No events available at the moment</h3>
                        <p>Check back later for new events!</p>
                    </div>
                <% } %>
            </section>
        </div>
    </main>

    <%- include('footer') %>
</body>
</html>