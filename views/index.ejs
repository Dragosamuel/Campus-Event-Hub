<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Campus Event Hub</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .hero {
            text-align: center;
            padding: 4rem 2rem;
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: white;
            border-radius: 16px;
            margin-bottom: 2.5rem;
            animation: fadeIn 1s ease-out;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
            position: relative;
            overflow: hidden;
        }
        
        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml;base64,');
            opacity: 0.1;
        }
        
        .hero h1 {
            font-size: 2.8rem;
            margin-bottom: 1.25rem;
            font-weight: 800;
            letter-spacing: -0.5px;
        }
        
        .hero p {
            font-size: 1.3rem;
            max-width: 700px;
            margin: 0 auto 1.75rem;
            font-weight: 300;
        }
        
        .cta-button {
            display: inline-block;
            background-color: #fff;
            color: #007bff;
            padding: 0.85rem 1.75rem;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            margin: 0.5rem;
            border: 2px solid transparent;
        }
        
        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
            border-color: rgba(0, 123, 255, 0.3);
        }
        
        .featured-events {
            margin-top: 2.5rem;
        }
        
        .section-title {
            text-align: center;
            margin-bottom: 2.5rem;
            color: #007bff;
            font-size: 1.8rem;
            font-weight: 700;
            position: relative;
        }
        
        .section-title::after {
            content: '';
            display: block;
            width: 60px;
            height: 4px;
            background: linear-gradient(90deg, #007bff, #0056b3);
            margin: 0.75rem auto;
            border-radius: 2px;
        }
        
        .events-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 2rem;
        }
        
        .event-card {
            background: white;
            border-radius: 16px;
            padding: 2rem;
            box-shadow: 0 6px 24px rgba(0,0,0,0.09);
            transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
            display: flex;
            flex-direction: column;
            border: 1px solid rgba(0,0,0,0.05);
            position: relative;
            overflow: hidden;
        }
        
        .event-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--primary-color), var(--primary-dark));
        }
        
        .event-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 16px 40px rgba(0, 0, 0, 0.15);
        }
        
        .event-card h3 {
            margin-top: 0;
            color: var(--primary-color);
            font-size: 1.4rem;
            font-weight: 600;
            line-height: 1.3;
        }
        
        .event-card p {
            color: var(--text-secondary);
            line-height: 1.6;
            margin: 0.6rem 0;
        }
        
        .event-card .btn {
            display: inline-block;
            margin-top: 1.25rem;
            background-color: var(--primary-color);
            color: var(--text-white);
            padding: 0.75rem 1.25rem;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            border: none;
            cursor: pointer;
            box-shadow: 0 2px 6px var(--shadow-light);
            align-self: flex-start;
        }
        
        .event-card .btn:hover {
            background-color: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px var(--shadow-medium);
        }
        
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2rem;
            }
            
            .hero p {
                font-size: 1rem;
            }
            
            .events-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <%- include('header') %>

    <main>
        <section class="hero">
            <h1>Welcome to Campus Event Hub</h1>
            <p>Your one-stop platform for discovering and managing campus events</p>
            <a href="events.ejs" class="cta-button">Explore Events</a>
            <a href="dashboard.ejs" class="cta-button" style="background-color: var(--success-color); color: var(--text-white);">Go to Dashboard</a>
        </section>

        <section class="featured-events">
            <h2 class="section-title">Featured Events</h2>
            <% if (typeof events !== 'undefined' && events.length > 0) { %>
                <div class="events-grid">
                    <% events.slice(0, 3).forEach(function(event, index) { %>
                        <div class="event-card fade-in">
                            <h3><%= event.title %></h3>
                            <p><%= event.description %></p>
                            <p><strong>Date:</strong> <%= event.date %> at <%= event.time %></p>
                            <p><strong>Location:</strong> <%= event.location %></p>

                            <a href="/register/<%= event._id %>" class="btn">Register</a>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <p class="fade-in">No events available at the moment. Check back later!</p>
            <% } %>
        </section>
    </main>

    <%- include('footer') %>
</body>
</html>